git status
git add *
git commit -m 'comments'
git push origin


git branch
git checkout master -> switch to existing branch

git pull origin -> fetch latest code from github and merge to master

git checkout -b new_branch -> create new branch
git checkout  quick-test -> switch to existing  quick-test
git merge master

git push origin

 git remote -v  --> git location


 
 
 
 ## Create new branch
 
 Step1: Update master branch with github
 git checkout master
 git pull origin --> master has now latest code 
 git checkout -b add_db_writer
 
 
 